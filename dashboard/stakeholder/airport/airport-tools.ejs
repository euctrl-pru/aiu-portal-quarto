```{=html}
<div class="grid list text-center" style="column-gap: 10px;">
    <% for (const item of items) { %>
    <div class="g-col-12 g-col-lg-6 g-col-xl-4" <%= metadataAttrs(item) %>>
        <div class="quarto-grid-item card h-100 card-center airport-tools">
            <div class="card-img-top">
                <a href="<%- item.href %>" alt="<%= item.description %>">
                    <img src="<%= item.image %>" class="thumbnail-image card-img" loading="lazy"
                        alt="<%= item.title %>">
                </a>
            </div>
            <div class="card-body">
                <a href="<%- item.href %>" class="quarto-grid-link">
                    <h5 class="no-anchor card-title listing-title"><%= item.title %></h5>
                </a>
                <div class="card-text">
                    <div class="listing-description">
                        <%= item.description %>
                    </div>
                    <div class="badge-container">
                        <span class="badge <%- item.badgeclass %>"><%= item.badge %></span>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <% } %>
</div>
<style>
    .card .card-body .card-text .badge-container {
        position: absolute;
        left: 1.2rem;
        bottom: 0.75rem;
        display: block;
    }
</style>
```
